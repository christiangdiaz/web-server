# Cherry-Pick

`e8112b2` on `lab09-feature` and `b0afc6b` on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.
